# Copy this file to .env.local and adjust if needed. Vite only exposes
# variables prefixed VITE_ to client code.

# Base URL of the ForexMind AI FastAPI backend (see ../README.md to run it).
VITE_API_BASE_URL=http://localhost:8000
